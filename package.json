{
  "name": "assignment",
  "version": "1.0.0",
  "description": "Takehome assignment for CaseWare",
  "main": "index.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "npm install && npm run build && npm run start-server ",
    "start-dev": "cd wwwroot/orderpage && npm run build && npm run start",
    "build": "cd wwwroot/orderpage && npm install && npm run build",
    "start-server": "node index.js"
  },
  "author": "Nolan Kingdon",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "luhn": "^2.4.1"
  }
}
